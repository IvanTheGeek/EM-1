can you create multiple light mode styles that would create the event modeling diagram idea of columns for each of the cases of the Slices files? I am trying to get a template. the data should be collapsed areas than can be expanded if desired, or a modal even. The screen will be a smaller snapshot of the real screen, so just a pic added. The GWT should be part of the Slice column, visability for the entire GWT block should be toggleable.    modern and clean as easy to us with touch screen.